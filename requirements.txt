# AWS SDK and Lambda Dependencies
# Keep boto3 and botocore versions compatible for AWS Lambda runtime
boto3==1.34.0          # AWS SDK for Python - required for all AWS service interactions
botocore==1.34.0       # Low-level interface to AWS services (boto3 dependency)

# Development and Testing
pytest==7.4.3          # Testing framework for unit and integration tests
pytest-mock==3.12.0    # Mock objects for testing AWS services

# HTTP Requests and Data Validation
requests==2.31.0       # HTTP library for external API calls and integrations
jsonschema==4.20.0     # JSON schema validation for API requests

# Additional Recommended Dependencies for Serverless Development
# Uncomment and add versions as needed:
# python-json-logger>=2.0.4    # Structured JSON logging for CloudWatch
# aws-lambda-powertools>=2.15.0    # AWS Lambda utilities for logging, tracing, and metrics
# pytz>=2023.3            # Timezone calculations and UTC conversions
# moto>=4.1.11           # Mock AWS services for local testing
# pydantic>=1.10.7       # Data validation and settings management 